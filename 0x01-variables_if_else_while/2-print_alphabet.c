#include <stdio.h>

/**
 * main - The main function is 'main'
 *
 * Return: The program returns 0
 */
int main(void)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		putchar(alph);
	}
	putchar('\n');

	return (0);
}
