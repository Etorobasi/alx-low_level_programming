#include <stdio.h>

/**
 * main - This is the main function
 *
 * return - The program returns 0 when run successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
